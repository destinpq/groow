# Production Environment Variables for Vercel

# API Configuration (Note: REACT_APP_ prefix is required for Create React App)
# But since we're using UMI, we'll use the standard naming
REACT_APP_API_URL=https://groow-api.destinpq.com/api/v1
API_URL=https://groow-api.destinpq.com/api/v1

# Environment
NODE_ENV=production

# Application Settings
APP_NAME=Groow
APP_VERSION=1.0.0

# Public URL
PUBLIC_PATH=/